module Lib
    ( 
      rabinKarp
    , rabinKarpMulti
    , parRabinKarpN
    , doMatch
    ) where


import StringMatch.RabinKarp (rabinKarp, rabinKarpMulti)
import StringMatch.Parallel  (parRabinKarpN)
import StringMatch.Match (doMatch)
